import React from 'react';
import Icon from './Icon';

const Header = () => {
	return (
		<div className="l-header">
			<Icon />
			<h1 className="l-header__title">Lista Pracowników</h1>
		</div>
	)
}

export default Header;
